// components/LoadingScreen.tsx

/*
import { motion } from "framer-motion";

export default function LoadingScreen() {
  return (
    <div className="fixed inset-0 z-50 bg-black flex items-center justify-center overflow-hidden">
      
      {/* Fog layers 
      <div className="absolute inset-0 bg-black">
        <motion.div
          className="absolute w-[200%] h-[200%]  bg-white rounded-full blur-3xl"
          animate={{ x: ["-25%", "25%"] }}
          transition={{ repeat: Infinity, duration: 10, ease: "linear" }}
        />
        <motion.div
          className="absolute w-[150%] h-[150%] bg-white rounded-full blur-2xl top-10 left-10"
          animate={{ x: ["20%", "-20%"] }} 
          transition={{ repeat: Infinity, duration: 15, ease: "linear" }}
        />
        <motion.div
          className="absolute w-[250%] h-[250%] bg-white rounded-full blur-3xl bottom-0 left-[-50%]"
          animate={{ x: ["-15%", "15%"] }}
          transition={{ repeat: Infinity, duration: 20, ease: "linear" }}
        />
      </div>
      </div>
  );
}*/
